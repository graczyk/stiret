CMAKE_MINIMUM_REQUIRED ( VERSION 2.8 )
PROJECT ( stiret )
FILE ( GLOB SRC *.cpp *.hpp)
FIND_PACKAGE ( SDL REQUIRED )
LINK_LIBRARIES ( ${SDL_LIBRARY} )
ADD_EXECUTABLE ( stiret WIN32 ${SRC} )
SET_TARGET_PROPERTIES ( stiret PROPERTIES COMPILE_FLAGS "-std=c++11 -g")
